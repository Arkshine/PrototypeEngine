add_sources(
	#TODO: this needs to be moved to a separate library at some point. Only the interface is shared.
	CCommandLine.h
	CCommandLine.cpp
	CNetworkBuffer.h
	CNetworkBuffer.cpp
	Common.h
	ICommandLine.h
	Logging.h
	Logging.cpp
	Platform.h
	Platform.cpp
)
